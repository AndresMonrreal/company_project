interface:
  display_name: "Create Hexagonal Feature"
  short_description: "Build feature with ports, adapters, tests"
  default_prompt: "Use $create-hexagonal-feature to add a manufacturing feature with domain, use cases, adapters, and tests."
policy:
  allow_implicit_invocation: true
