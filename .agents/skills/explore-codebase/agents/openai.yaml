interface:
  display_name: "Explore Codebase"
  short_description: "Map hexagonal modules and adapters"
  default_prompt: "Use $explore-codebase to find the real files, packages, and conventions relevant to a feature."
policy:
  allow_implicit_invocation: true
